$( function () {

    var width = $(window).width(); //retrieve current window width
    var height = $(window).height(); //retrieve current window height
    console.log("windows width " + width);
    console.log("windows height " + height);

    $('#bg').css('-webkit-transform', 'scale(0.5, 0.5) translate(-400px,-400px');

});

